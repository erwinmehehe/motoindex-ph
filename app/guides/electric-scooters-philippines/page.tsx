import type { Metadata } from "next";
import Link from "next/link";
import { Breadcrumbs } from "@/components/Breadcrumbs";
import { electricMotorcycles, php } from "@/lib/electricMotorcycles";
import { pageMetadata } from "@/lib/site";
export const metadata:Metadata=pageMetadata({title:"Electric Scooters Philippines: Verified Models",description:"Compare locally launched electric scooters in the Philippines by price, battery setup, claimed range, charging time, speed and registration category.",path:"/guides/electric-scooters-philippines"});
export default function Page(){return <section className="page shell"><Breadcrumbs items={[{label:"Guides",href:"/guides"},{label:"Electric scooters"}]}/><div className="page-head"><h1>Electric scooters in the Philippines</h1><p>The current verified set includes three VinFast electric scooters officially launched in the Philippines and classified by LTO as L3 motorcycles.</p></div><div className="catalog-grid">{electricMotorcycles.map(m=><Link className="catalog-card" href={`/motorcycles/electric/${m.slug}`} key={m.slug}><img src={m.imageUrl} alt={`${m.make} ${m.model}`}/><h2>{m.make} {m.model}</h2><strong>{php(m.priceFromPhp)}</strong><p>{m.rangeTwoKm} km claimed two-battery range · {m.topSpeedKph} km/h</p></Link>)}</div><div className="source-panel"><h2>Battery subscription or battery ownership</h2><p>The lowest advertised prices use battery subscription. Buying one or two batteries increases the upfront amount but changes the ownership arrangement. Compare the written subscription terms, swapping access, warranty and total monthly cost before choosing.</p></div></section>;} 