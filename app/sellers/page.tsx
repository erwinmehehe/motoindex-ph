import type { Metadata } from "next";
import { pageMetadata } from "@/lib/site";
import Link from "next/link";
import { dealerCities, citySlug, sellerCounts, sellers } from "@/lib/sellers";
export const metadata: Metadata = pageMetadata({ title: 'Motorcycle Dealers & Gear Sellers Philippines', description: 'MotoIndex seller directory for motorcycle dealers, helmet shops, tire sellers and accessory retailers in the Philippines.', path: '/sellers', index: false });
export default function SellersPage(){const counts=sellerCounts();return <section className="page shell"><div className="page-head"><h1>Dealers & motorcycle shops</h1><p>Browse motorcycle dealers, helmet shops, tire sellers and accessory retailers. Sample business profiles are labeled. Confirm current contact and inventory details directly with the shop.</p></div><div className="seller-stats"><div><strong>{counts.total}</strong><span>Seller profiles</span></div><div><strong>{counts.dealers}</strong><span>Dealer records</span></div><div><strong>{counts.retailers}</strong><span>Retailer records</span></div><div><strong>{counts.cities}</strong><span>Cities</span></div></div><div className="section-head"><div><h2>Browse seller profiles</h2></div></div><div className="seller-grid">{sellers.map(s=><Link key={s.slug} href={`/sellers/${s.slug}`}><div className="seller-icon">{s.type==="dealer"?"D":"S"}</div><span className="catalog-status">sample</span><h3>{s.name}</h3><p>{s.city} · {s.region}</p><small>{s.brands.join(" · ")}</small><b>View profile →</b></Link>)}</div><div className="section-head"><div><h2>Dealer locations</h2><p>City pages currently show sample dealer profiles where available.</p></div></div><div className="city-chips">{dealerCities().map(c=><Link key={c} href={`/dealers/${citySlug(c)}`}>{c}</Link>)}</div></section>}
