import type { Metadata } from "next";
import { pageMetadata } from "@/lib/site";
import Link from "next/link";
import { recommendationGuides, getRecommendationModels, isIndexableRecommendation } from "@/lib/data";
import { observedMarketPriceLabel } from "@/lib/marketChecks";
const publicGuides = recommendationGuides.filter(g=>isIndexableRecommendation(g.slug));
export const metadata: Metadata = pageMetadata({ title: 'Motorcycle Buying Guides Philippines', description: 'Compare current Philippine motorcycles by budget, seat height, weight, fuel economy, ABS and riding use.', path: '/recommendations', index: publicGuides.length>0 });
export default function RecommendationsPage(){return <section className="page shell"><div className="page-head"><h1>Compare motorcycles by budget, size and use.</h1><p>Use the current buying guides to narrow your shortlist, then open the exact motorcycle page for prices, specs and dealer options.</p></div><div className="guide-grid"><Link href="/recommendations/electric-motorcycles-philippines" className="guide-card"><span className="section-kicker">Electric</span><h2>Electric motorcycles in the Philippines</h2><p>Compare verified electric models by price, battery setup, claimed range, charging time and top speed.</p><strong className="guide-link">Compare electric motorcycles →</strong></Link>{publicGuides.map(g=>{const models=getRecommendationModels(g.slug);return <Link key={g.slug} href={`/recommendations/${g.slug}`} className="guide-card"><h2>{g.title}</h2><p>{g.description}</p><div className="guide-preview">{models.slice(0,3).map(m=><span key={m.id}>{m.make} {m.model} <b>{observedMarketPriceLabel(m)}</b></span>)}</div><strong className="guide-link">Open guide →</strong></Link>})}</div></section>}
