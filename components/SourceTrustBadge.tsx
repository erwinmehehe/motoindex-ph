export function SourceTrustBadge({
  label,
  url,
  needsRecheck = false,
  historical = false,
  compact = false,
}: {
  label?: string;
  url?: string;
  needsRecheck?: boolean;
  historical?: boolean;
  compact?: boolean;
}) {
  void label;
  void url;
  void needsRecheck;
  void historical;
  void compact;
  return null;
}
